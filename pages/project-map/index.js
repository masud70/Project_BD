import React from "react";
import Map from "./Map";
import * as data from "./data";

const ProjectMap = () => {
    return (
        <div>
            <Map data={data}/>
        </div>
    );
};

export default ProjectMap;
